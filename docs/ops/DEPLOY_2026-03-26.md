# Deploy Log 2026-03-26

Registro operativo de la ejecucion hecha el 2026-03-26 para publicar cambios actuales de Multicel.

## Objetivo

- Actualizar backend productivo en Hostinger con los cambios locales vigentes.
- Validar frontend localmente y generar un deploy web reproducible en Vercel.

## Validacion local previa

- Backend tests:
  - comando: `npm.cmd --prefix backend/server test -- --runInBand`
  - resultado: `15` suites OK, `135` tests OK
- Frontend typecheck:
  - comando: `npm.cmd --prefix frontend-react run typecheck`
  - resultado: OK
- Frontend build:
  - comando: `npm.cmd --prefix frontend-react run build`
  - resultado: OK

## Backend productivo en Hostinger

Flujo usado: paquete `.tgz` + backup + `rsync` + `npm run migrate` + `pm2 restart`.

- paquete local generado:
  - `C:\Users\User\OneDrive\Desktop\multicell\.codex-ssh\backend-server-deploy-20260326.tgz`
- paquete remoto subido:
  - `/home/ubuntu/backend-server-deploy-20260326.tgz`
- backup de codigo generado:
  - `/home/ubuntu/kaisen/backend-before-20260326145114`
- backup de base generado:
  - `/home/ubuntu/backups/20260326145114_sistema_gestion.sql.gz`
- migracion aplicada:
  - `V24__vendor_compensation`
- proceso reiniciado:
  - `pm2 restart kaisen-backend`
- healthcheck final:
  - `http://127.0.0.1:3000/api/readyz`
- resultado final:
  - `status=ok`
  - `environment=production`
  - `db.status=connected`

## Frontend en Vercel

Estado local encontrado antes del deploy:

- no habia `vercel` CLI instalada en PATH
- no habia configuracion local en `C:\Users\User\.vercel`
- no habia configuracion local en `C:\Users\User\AppData\Roaming\Vercel`
- no habia variables de entorno `VERCEL_*` cargadas en esta maquina

Accion tomada:

- se uso el fallback de la skill `vercel-deploy` para crear un deploy preview del frontend actual

Resultado:

- preview URL:
  - `https://skill-deploy-up0deyalgk-codex-agent-deploys.vercel.app`
- claim URL:
  - `https://vercel.com/claim-deployment?code=200eb779-cce1-4dbb-b4d5-4cdaf3a992bf`
- deployment id:
  - `dpl_EBFhZEL1Y9M34D2j9qwMpEXQVNC5`
- estado:
  - preview generado

Pendiente para convertir este frontend en produccion del proyecto actual:

- hacer deploy autenticado al proyecto Vercel real, o
- reclamar el deploy generado y asociarlo al proyecto/dominio correcto

## Observaciones operativas

- `frontend-react/vercel.json` contiene una rewrite de `/api/:path*` hacia `http://72.60.14.52/api/:path*`
- el backend que se inspecciono y actualizo hoy en Hostinger corre en `163.176.174.59`
- antes del proximo deploy productivo del frontend conviene verificar si la web actual depende de:
  - esa rewrite fija en `vercel.json`, o
  - variables `VITE_API_URL` / `VITE_API_BASE_URL` configuradas en Vercel

## Siguiente paso recomendado

- dejar autenticado o linkeado el proyecto Vercel actual para poder hacer deploy productivo del frontend sin depender de un preview claimable

## Actualizacion posterior del mismo dia

Mas tarde, el 2026-03-26, se agrego una capa de routing publico en el VPS:

- `kaisen-backend` paso de `3000` a `3100`
- se creo `kaisen-public-router` en `3000`
- `cliente2` quedo publicado por:
  - `http://163.176.174.59:3000/cliente2/api`

Eso habilita crear un proyecto Vercel separado para `cliente2` sin abrir mas puertos publicos.
